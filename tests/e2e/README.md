# e2e tests

End-to-end tests validate deployed topology profiles per `TEST_SPEC.md` and `APP_RUNTIME_TOPOLOGY_SPEC.md`.

Use `pnpm dev:server:postgres:unified-process:standalone` for local unified-process verification.
